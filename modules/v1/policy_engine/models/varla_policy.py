from enum import IntEnum


class VarlaPolicy(IntEnum):
    """VarlaPolicy is a base class that represents the policies that are used in the system."""

    ...


# Example of a policy class

# class AuthenticationPolicy(VarlaPolicy):
#     """AuthenticationPolicy is a class that represents the policies that are used in the system."""

#     all = -1
#     canLogin = 1
#     canResetPassword = 2
